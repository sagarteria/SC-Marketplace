/*
 * BoardsPage Messages
 *
 * This contains all the text for the BoardsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BoardsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BoardsPage container!',
  },
});
